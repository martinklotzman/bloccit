<h1>All Advertisements</h1>

<% @advertisements.each do |advertisement| %>
  <div class="media">
    <div class="media-body">
      <h4 class="media-heading">
        <p><%= link_to "$#{advertisement.price}: #{advertisement.title}", advertisement %></p>
      </h4>
    </div>
  </div>
<% end %>
